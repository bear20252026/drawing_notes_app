# 增量回归审计（2026-09-16）

> **基线**：`b45c1f1`（v1.17.0 全量审计发版）→ `5693282`（v1.17.6+74，HEAD）+ **5 处未提交改动 + 1 个新测试**。
> **范围**：设计规范回归 / 动效（Emil 八类）/ 三输入无障碍 / 液态玻璃 / 变更区异步与质量 / 版本文档对齐。
> **方法**：主会话静态扫描（子代理因外目录权限拦截未能并行）；**未跑** `dart analyze` / `flutter test`（本环境 PATH 无 Dart/Flutter SDK）。
> **性质**：只读审计。修复须你确认后再动。

---

## 一、总体评价

09-07 全量审计后的 6 个发版（v1.17.1–1.17.6）质量方向正确：

| 维度 | 结论 |
|---|---|
| 审计未决闭环 | 时区 LWW、密码写队列独占（`_runDocExclusive`）、CI SHA pin、FakeClock 已落地 |
| i18n | 四批推进，硬编码 1070→约 375（doc 域），arb 198→829 键 |
| lint 防回退 | `unawaited_futures` + `avoid_slow_async_io` 已写入 `analysis_options.yaml` |
| 版本一致性 | `pubspec 1.17.6+74` / `iss MyAppVersion 1.17.6` / CHANGELOG 对齐 |
| 动效硬规则 | **未发现** `Curves.easeIn*`、`scale(0)`、UI 层 `Duration ≥ 300ms` 新违规 |
| 三输入 | 4 处 `onSecondaryTap*` 均已配对长按；`reduceMotionOf` / 高对比链路完整 |

**未提交工作区**正在闭环 09-07 的三条 📋（B3 色板键盘调色 / B10+B13 块 Alt+排序 / B3 裁剪键盘微调），方向正确，但合入前有 **P0 测试缺陷** 与 **P1 lint/规范缺口** 必须先修。

---

## 二、发现清单

### P0（合入前必修）

| # | 位置 | 证据 | 问题 | 建议 |
|---|---|---|---|---|
| P0-1 | `test/shared/widgets/color_picker_rgb_input_test.dart:9-32` | `pumpPicker` 在 `tap(open)+pumpAndSettle` 后立即 `return picked`；`picked` 由对话框 **关闭后** 的 `showDialog` future 赋值 | `late final Color? picked` 在返回时尚未赋值 → `LateInitializationError`。**三个 testWidgets 均无法通过** | helper 只负责「打开对话框」，不返回结果；由各测试在 `pop` 后读外部变量；或 helper 接受回调在关闭后断言 |

### P1（规范/lint 违规）

| # | 位置 | 证据 | 问题 | 建议 |
|---|---|---|---|---|
| P1-1 | `lib/features/drawing/presentation/editor_page_body.dart:125` | `_confirmCrop();` 裸调用（返回 `Future<void>`） | v1.17.6 已启用 `unawaited_futures`；同文件新快捷键路径已用 `unawaited(_confirmCrop())`（`editor_page_shortcuts.dart:95`），此处不一致且会挡 analyze | 改为 `unawaited(_confirmCrop());` |
| P1-2 | `lib/shared/widgets/color_picker_dialog.dart:150-156` | `OutlineInputBorder(...)` 未指定 `borderRadius` | Flutter 默认圆角 **4**，不在合法档位 `0/5/8/11/18/pill`（AGENTS.md / DESIGN.md:127-135） | `borderRadius: BorderRadius.all(Radius.circular(AppleRadius.xs))`（5）或 `sm`（8） |
| P1-3 | `lib/features/drawing/presentation/editor_components.dart:97,692,882` | `Color(0x8842A5F5)` / `0x3342A5F5` / `0xFF42A5F5` | UI 铬层（连接线/选区填充/手柄）仍用 Material 杂散蓝。09-07 A1 声称「17 处 42A5F5 收敛为 actionBlue」——**画布 UI 铬未收干净** | 统一 `AppleColor.actionBlue`（#0066CC）+ `.withValues(alpha:)`；深色模式用 `actionBlueOnDark` |
| P1-4 | `lib/features/doc/doc_editor_blocks.dart:370,375` | `TextStyle(fontSize: 18)` / `fontSize: 16` | 手写字号，绕过 `AppleType`（A7/A8 在 09-07 已系统性令牌化，此处回潮或遗留） | `AppleType.controlStyle` / `titleStyle.copyWith` 按语义取档 |

### P2（建议）

| # | 位置 | 证据 | 问题 | 建议 |
|---|---|---|---|---|
| P2-1 | `color_picker_dialog.dart:135-140` | `TextField` **无 `onSubmitted` / `textInputAction`** | 注释写「Enter/失焦提交」，实际只靠 FocusNode 失焦监听（:89）。桌面 Enter 若未触发 unfocus 则不提交；测试用 `TextInputAction.done` 间接触发 | 补 `textInputAction: TextInputAction.done` + `onSubmitted: (_) => _submitRgb()` |
| P2-2 | `color_picker_dialog.dart:140` | `style: textTheme.bodySmall` | 排版层级应走 control 13 / caption 11.5，不走 M3 `bodySmall` | `AppleType.controlStyle(...)` |
| P2-3 | `color_picker_dialog.dart:131` vs `:134` | 注释「56px 宽」实际 `width: 76` | 文档漂移 | 改注释 |
| P2-4 | `color_picker_dialog.dart:142-157` | RGB 三格无 `Semantics(label:)` | 读屏只能听到 prefix 字符 | 包 `Semantics(label: 'R 通道')` 等（已有 l10n 习惯） |
| P2-5 | `lib/features/doc/doc_page_widgets.dart:396` | `TextStyle(fontSize: 13, ...)` | 同 P1-4，应走 `AppleType.controlStyle` | 令牌化 |
| P2-6 | `lib/features/drawing/presentation/editor_components.dart:780` | `TextStyle(fontSize: 9, color: Colors.black54)` | 手写字号 + 硬编码灰 | 若属画布内容叠加层可豁免，建议注释标明「canvas content layer」 |
| P2-7 | `lib/core/canvas_model/page_connector.dart:10,29` | 默认 `0xFF42A5F5` | 数据模型默认连接色仍是 Material 蓝（持久化兼容需迁移策略） | 新建默认改 `actionBlue.toARGB32()`；读侧兼容旧值 |
| P2-8 | `lib/core/theme/apple_design.dart` vs `AGENTS.md §4` | 总纲已裁决玻璃 **底色 0.62 / saturate 1.4**（`DESIGN_SYSTEM.md` 208-210、223 旧文「80%/180」已标废弃） | `AGENTS.md` 仍写「必须含 80% 底色」 | 同步 AGENTS.md 到 0.62，避免双源打架 |
| P2-9 | `lib/features/drawing/presentation/canvas_painter.dart:335,361,521` | 纸型网格/参考线 `42A5F5` 系透明色 | **内容层**纸型纹理，非 UI 铬；可保留，但建议挂 `AppleColor` 或具名常量，避免下次 A1 扫描误报 | 抽 `_kPaperLineBlue` 或注明 domain exemption |

### 通过 / 关闭项（无新发现）

| 项 | 结论 |
|---|---|
| 曲线白名单 | 全库无 `Curves.easeIn` / `easeInCubic` 等禁用项（`apple_motion.dart` 除外） |
| UI 时长 &lt;300ms | 超 300ms 命中均为非 UI 反馈：skeleton 循环 1200ms、tooltip **wait** 450ms、搜索防抖 300ms、历史防抖 500ms、激光笔 700/1800ms（罕见工具） |
| `scale(0)` 入场 | 无 |
| 右键三输入 | `all_doc_row` / `home_page_widgets` / `editor_page_overlays` / `editor_text_overlays` 均 secondary+longPress 配对 |
| 减弱动效 | `ApplePressable` / `apple_sheet_fade_route` / `glass_surface` / `liquid_glass_shader` L3→L2 / skeleton 已接 |
| 玻璃性能闸门 | `LiquidGlassGate.resolve`：未就绪或 reduceMotion → L2，符合技术方案 |
| 玻璃参数 | `sigma=12`、`saturate=1.4`、`opacity=0.62/0.45`、`displacement=70`、`aberration=2` 与 09-05 技术方案一致（旧 180/80% 已废弃） |
| 未提交 a11y 正向 | Alt+↑/↓ 仅顶层块平移（防误跨层）；裁剪 1px/Shift10px + Enter/Esc；RGB 输入 digitsOnly + 0–255 钳制 + 失焦提交 + Focus Blue 2px |
| 密码写队列 | `setFilePassword` / `changeFilePassword` / `bindFileUsbSlot` / `resetFilePasswordWithUsb` 均挂 `_runDocExclusive` |
| 版本三处 | 1.17.6 一致 |
| `geometry_engine.dart` | 仅 format 换行，无逻辑变更 |

---

## 三、上轮 📋 闭环状态（对照 AUDIT_FULL_2026-09-07 §四）

| 📋 项 | 状态 | 证据 |
|---|---|---|
| E1 i18n | 🔧 进行中（约 65%） | v1.17.2–1.17.5 四批 |
| G15 时区 LWW | 🔧 已落地 | v1.17.1 + `time_serialization.dart` + 测试 |
| G14 密码写队列 | 🔧 已落地 | v1.17.1 `storage_service_file_password.dart` |
| F12/C9 lint 启用 | 🔧 已落地 | v1.17.6 `analysis_options.yaml:27-28` |
| B3 色板键盘调色 | 🔧 **未提交** | `color_picker_dialog.dart` RGB 三格 + 新测试（测试本身 P0-1） |
| B3 裁剪键盘微调 | 🔧 **未提交** | `editor_page_shortcuts.dart:76-104,182-201` |
| B10/B13 块 Alt+排序 | 🔧 **未提交** | `doc_editor_blocks.dart:532-549` |
| B3 小地图键盘平移 | 📋 仍未做 | — |
| B11 全库 Semantics 普查 | 📋 仍未做 | RGB 三格亦缺 label（P2-4） |
| F3 死代码删除 | 📋 待用户同意 | — |
| J5/J6 CI SHA pin | 🔧 v1.17.1 | — |

---

## 四、修复建议排序（待你确认后执行）

1. **P0-1** 修测试 helper（否则未提交 a11y 工作无法用测试证明）
2. **P1-1** `_confirmCrop` → `unawaited`（否则 analyze 门禁红）
3. **P1-2** RGB 输入框圆角令牌化
4. **P1-3** 画布 UI 铬 42A5F5 → `actionBlue`（3 处）
5. **P1-4 + P2-2/P2-5** 手写 `TextStyle` 令牌化
6. **P2-1** RGB `onSubmitted` 补齐
7. **P2-8** AGENTS.md 玻璃 80%→0.62 同步
8. 其余 P2 按你意愿打包

---

## 五、验证缺口（合入前必做）

本审计环境 **无 Dart/Flutter SDK**，以下未执行：

```bash
cd .../drawing_notes_app
dart analyze
flutter test test/shared/widgets/color_picker_rgb_input_test.dart
flutter test   # 全量
```

未提交改动在修完 P0-1/P1-1 前，**不应**合入。

---

## 六、方法备注

- 会话 workdir 为 `tmp_design_refs`，子代理访问 `../drawing_notes_app` 被 `external_directory` 拦截，故 A–F 维度由主会话串行完成。
- 扫描命令以 `rg` 模式匹配为主，对 `lib/**`（排除 `l10n`）全量；未逐文件通读。
- 画布内容层（纸型网格、用户笔迹色、PDF 导出白底）按 domain 豁免，仅 UI 铬 / 令牌层计违规。
