# 增量回归审计（2026-09-18）

> **基线**：`5693282`（v1.17.6+74，HEAD）+ **9 处未提交改动 + 1 个新测试**。
> **范围**：设计规范回归 / 动效硬规则 / 三输入无障碍 / 液态玻璃边界 / 异步与 lint / 版本三处一致性；对照 `AUDIT_2026-09-16.md` 闭环。
> **方法**：静态扫描（rg + 关键文件精读）+ **真实门禁**（Flutter SDK `D:\flutter`）：`flutter analyze`、`flutter test` 全量。
> **性质**：只读审计。**未改任何业务/测试代码**。修复须你确认后再动。

---

## 一、总体评价

09-16 审计后的未提交工作方向正确，多数 P0/P1 已落地：

| 维度 | 结论 |
|---|---|
| 09-16 P0-1 测试 LateInit | ✅ 已修（`ValueNotifier holder` 模式） |
| 09-16 P1-1 `_confirmCrop` 裸调用 | ✅ 已修（body:125 + shortcuts:95 均 `unawaited`） |
| 09-16 P1-2 RGB 输入圆角 | ✅ 已修（`AppleRadius.xs` + Focus Blue 2px） |
| 09-16 P1-3 UI 铬 42A5F5 | ✅ 已修（`editor_components` 三处 → `AppleColor.actionBlue`） |
| 09-16 P1-4 手写 fontSize | 🔧 半修（改为 `AppleType.*.copyWith(fontSize:)`，字号仍手写） |
| 09-16 P2-1/2/8 | ✅ onSubmitted / controlStyle / AGENTS 玻璃 0.62 |
| 09-07 B3 色板键盘 / B10+B13 块排序 / B3 裁剪键盘 | 🔧 已实现（未提交） |
| 版本三处 | ✅ `pubspec 1.17.6+74` / iss `1.17.6` / CHANGELOG `## [1.17.6]` |
| 动效硬规则 | ✅ 无 `Curves.easeIn`、无 `scale(0)` 实现 |
| 三输入右键 | ✅ 4 处 secondary 均有长按配对 |

**但合入前仍有 1 条 P0 门禁红灯**：新测试 `color_picker_rgb_input_test.dart` **无法编译**，导致 `flutter analyze` 与 `flutter test` 均失败。其余 **1974** 个用例全绿——阻塞点单一、修复成本极低。

另有内容层玻璃与非法圆角等规范回归，以及 `unawaited_futures` 的 **lint 覆盖面缺口**（见 P1-1：开关拨了，但非 async 调度器扫不到）。

---

## 二、门禁结果（本轮实测）

| 门禁 | 命令 | 结果 |
|---|---|---|
| analyze | `flutter analyze --no-pub`（`D:\flutter\bin\flutter.bat`） | ❌ **1 error** — `color_picker_rgb_input_test.dart:95` `undefined_getter`（`BorderRadius` 无 `.x`）；约 351s |
| test 全量 | `flutter test --no-pub --reporter compact` | ❌ **+1974 −1** — 唯一失败：同文件 **Compilation failed**；约 4 分 10 秒 |
| test 单文件 | `flutter test test/shared/widgets/color_picker_rgb_input_test.dart` | ❌ 同编译错误 |
| 版本三处 | pubspec / iss / CHANGELOG | ✅ 1.17.6 一致 |

环境备注：PATH 无 `flutter`；须 `Set-Location` 到 `drawing_notes_app` 后调用 `D:\flutter\bin\flutter.bat`，并预置 `ProgramFiles(x86)`（否则 bat 报 env 缺失）。analyze 输出经 PowerShell 以 NativeCommandError 包装 stderr，EXIT 码仍可信。

---

## 三、发现清单

### P0（合入前必修 — 门禁红）

| # | 位置 | 证据 | 问题 | 建议 |
|---|---|---|---|---|
| P0-1 | `test/shared/widgets/color_picker_rgb_input_test.dart:93-95` | `border.borderRadius.resolve(null)` 的静态类型是 **`BorderRadius`**（`BorderRadiusGeometry.resolve` → `BorderRadius`），不是 `Radius`；`.x` 只在 `Radius` 上 | **analyze error + 测试编译失败**；全量套件唯一红灯。意图是断言圆角落在合法档位（xs/sm/md/lg） | `radius.topLeft.x`（OutlineInputBorder 均匀圆角，四角同值）；或 `(border as OutlineInputBorder).borderRadius.resolve(null).topLeft.x` |

### P1（规范/lint 缺口）

| # | 位置 | 证据 | 问题 | 建议 |
|---|---|---|---|---|
| P1-1 | `analysis_options.yaml:27` + `editor_page_actions.dart:9-51` + `editor_page_shortcuts.dart:166,170` | lint `unawaited_futures` **仅对 async 函数体**生效；`_onMainMenuSelected` 为 **同步 `void`**，switch 中 `_exportPng/_exportSvg/_exportPdf/_exportText/_exportWordCompatibleRtf/_copyPngToClipboard/_exportPptx/_exportJson/_createChart/_showCommandPalette/_showStats/_openShapeLibrary/_showShortcutHelp`（及 shortcuts 的 `_showCommandPalette`）均为裸 `Future<void>` 调用 | 门禁 **不会红**（本轮 analyze 仅报 P0-1），但与已改的 `_confirmCrop`/shortcuts:95 的 `unawaited(...)` 风格不一致；菜单动作 fire-and-forget 是有意还是遗漏无文档 | 统一包 `unawaited(...)`（与 F12/C9「14 处显式化」口径对齐），或在调度器旁注明「有意不等待」；**可选**，非 analyze 硬阻塞 |
| P1-2 | `schedule_page.dart:522`（`_EventCard`）、`:482`（`_emptyState`） | `GlassSurface` 用于日程**列表内容卡片**与空态面板 | 违反 `DESIGN_SYSTEM.md:196-201` **硬边界**：「内容层（一个像素都不加）… 每张内容卡片保持扁平」；`glass_surface.dart:5-9` 同源表述 | `_EventCard` → 扁平表面（`scheme.surface` / Card）；`_emptyState` 一并扁平，或若视为「页内浮层」须在 DESIGN_SYSTEM 裁决表显式豁免并注释 |
| P1-3 | 见下表 9 处 | `border: OutlineInputBorder()` / `const OutlineInputBorder()` **未指定 borderRadius** → Flutter 默认 **4**，不在合法档位 `0/5/8/11/18/pill` | 与 09-16 P1-2（RGB 框已修）同类回潮/遗留；输入框视觉不统一 | `BorderRadius.all(Radius.circular(AppleRadius.xs))`（5）或按控件语义取 `sm`；建议抽共享 `InputDecoration` 助手避免再漏 |

**P1-3 清单**：

| 文件 | 行 |
|---|---|
| `lib/features/doc/presentation/table_editor_widget.dart` | 238 |
| `lib/features/drawing/presentation/editor_page_dialogs.dart` | 183 |
| `lib/features/drawing/presentation/shape_library.dart` | 162 |
| `lib/features/drawing/presentation/editor_page_actions.dart` | 137 |
| `lib/features/drawing/presentation/editor_page_editing.dart` | 597 |
| `lib/features/notes/presentation/notebook_view_page_widgets.dart` | 303, 387, 447 |
| `lib/shared/widgets/unlock_sheets.dart` | 236 |

### P2（建议 / 待裁决）

| # | 位置 | 证据 | 问题 | 建议 |
|---|---|---|---|---|
| P2-1 | `color_picker_dialog.dart:132-165`（`_rgbField`） | 仅 `prefixText: '$label '`，无 `Semantics(label:)` | 读屏只能听到单字母；色板格有 Semantics，RGB 三格没有（09-16 P2-4 未修） | `Semantics(label: …)` 或 `InputDecoration.labelText`/`helperText` 配合 l10n |
| P2-2 | `glass_surface.dart:139-140` vs `:70-73` / `liquid_glass_shader.dart:127` | 文档注释写「默认 80%（DESIGN.md:396-398）」；实际 `surfaceOpacity = LiquidGlassRecipe.kRegularOpacity`（**0.62**），字段注释 `:70-73` 已更正 | 注释双源打架，升级液态玻璃时易再抄错 | 将 `:139` 改为与 `:70-73`/AGENTS §4 一致的 0.62 表述 |
| P2-3 | `editor_page_shortcuts.dart:196-202`（`_exitCropMode`） | 手写 `cropItem = null; _cropRect = null` | `interaction_state.dart` 已有 `clearCrop()` API（:210），未复用，行为可能漂移 | 改为 `_canvasInteraction.clearCrop()` + 清 `_cropRect` |
| P2-4 | `editor_page_canvas_surface.dart:318-320` | 小地图仅 `onTapDown` / `onPanUpdate`，无 `Semantics`、无键盘平移入口 | 09-07 B3「小地图键盘」仍未做；三输入硬性要求 | 焦点 + 方向键平移视口，或提供 ⋮/按钮等价入口 |
| P2-5 | `apple_design.dart:537-539` | 搜索框 `focusedBorder`: `scheme.primary, width: 1.2` | 与 AGENTS「Focus Blue `#0071E3` + **2px**」（DESIGN.md:300、440）字面不一致 | **待裁决**：若属 Apple 搜索控件特例，请在 DESIGN_SYSTEM 冲突表登记豁免；否则改 Focus Blue 2px |
| P2-6 | `core/canvas_model/page_connector.dart:10,29` | 默认连接色仍 `0xFF42A5F5` | 数据模型默认值（持久化兼容）；UI 铬已收敛，模型默认未跟 | 新建默认 → `AppleColor.actionBlue`；读侧兼容旧值（09-16 P2-7 未动） |
| P2-7 | `canvas_painter.dart:335,361,521` 等 | 纸型网格/小地图填充 `42A5F5` 系 | **内容层/domain 豁免**合理，但裸字面量易被后续扫描误报 | 具名常量 `_kPaperLineBlue` 或注释 `// domain: paper texture` |
| P2-8 | `doc_editor_blocks.dart:373,383` 等 | `AppleType.*.copyWith(fontSize: 18/16)` 等 | 排版令牌外手写字号（约 23 处残留，PDF `pw.TextStyle` 可豁免） | 按语义映射到 `AppleType` 档位，或在 token 层增补确实需要的字号 |
| P2-9 | `editor_components.dart:779` 等 | `TextStyle(fontSize: 9, …)` | 同上；画布内容叠加层可豁免但应注释 | 注明 canvas content layer 或并入 domain 字号 |

### 通过项（本轮未再发现新违规）

| 项 | 结论 |
|---|---|
| 动效曲线 / `scale(0)` / UI≥300ms | 无新违规（与 09-16 一致） |
| 右键三输入配对 | `all_doc_row` / `home_page_widgets` / `editor_overlays` / `text_overlays` 均有长按 |
| 玻璃配方数值 | regular 0.62 / clear 0.45 / sigma 12 / saturate 1.4 与技术方案一致（实现层正确，仅 P2-2 注释漂移） |
| `AppDesign.cardRadius/controlRadius` | 映射到 `AppleRadius.lg/md`，非第二套圆角体系 |
| `geometry_engine.dart` | 未提交 diff 为格式向，无逻辑变更迹象 |
| 架构测试 | 全量套件中 architecture_test（层方向 / 循环依赖 / Martin 耦合）通过 |
| KDF/benchmark | 套件内 PBKDF2/Argon2 基准用例通过 |

---

## 四、上轮 📋 闭环状态（对照 09-16 §三 与 09-07 未决）

| 项 | 状态 | 证据 |
|---|---|---|
| P0-1 RGB 测试 LateInit | ✅ 已修 | holder 模式；**新 P0** 为圆角断言 API 误用 |
| B3 色板键盘调色 | 🔧 未提交已实现 | RGB 三格 + digitsOnly + 0–255；Semantics 仍缺（P2-1） |
| B3 裁剪键盘微调 | 🔧 未提交已实现 | shortcuts:76-103 微调；:196-201 退出未复用 API（P2-3） |
| B10/B13 块 Alt+↑↓ | 🔧 未提交已实现 | `doc_editor_blocks.dart` 顶层块排序 |
| B3 小地图键盘平移 | 📋 仍未做 | P2-4 |
| B11 Semantics 普查 | 📋 仍未做 | RGB 三格即反例 |
| F12/C9 lint 防回退 | 🔧 已启用但覆盖不全 | 见 P1-1：同步调度器不被 lint 扫描 |
| F3 死代码删除 | 📋 待你同意 | 铁律：删改须先确认 |
| E1 i18n（09-07） | 🔧 进行中 | 约 375 处 doc 硬编码量级（历史数据，本轮未重扫） |
| F1/F9 长文件 / D11/D12 架构 | 📋 未决 | 非本轮增量范围 |
| 版本三处 | ✅ | 1.17.6 一致，本轮无 bump |

---

## 五、修复建议排序（**待你确认后执行**）

1. **P0-1** 测试断言：`radius.x` → `radius.topLeft.x`（1 行；解锁 analyze + test 全绿）
2. **复跑门禁**：`flutter analyze`（须 No issues）+ `flutter test`（须全绿）
3. **P1-2** 日程 `_EventCard`（及 `_emptyState`）去玻璃 → 扁平内容层
4. **P1-3** 9 处 bare `OutlineInputBorder` 补合法圆角（建议一次改完）
5. **P1-1**（可选）菜单/快捷键 Future 调用统一 `unawaited(...)` 或文档化
6. **P2-1** RGB `Semantics`；**P2-2** 玻璃注释对齐 0.62
7. **P2-3~P2-9** 按你意愿打包（含 apple_design 搜索框焦点条待裁决）

> 铁律提醒：任何删改/重构须你点头；**增量新增不受限**。本报告本身只读，未动代码。

---

## 六、方法与验证备注

- 项目目录：`C:\Users\17296\WorkBuddy\2026-08-29-23-43-00\drawing_notes_app`（shell 须先 Set-Location，避免静默分析外层目录）。
- Flutter：`D:\flutter\bin\flutter.bat`；rg：`$env:MIMO_RIPGREP_PATH`。
- 09-16 报告称「本环境无 SDK」——**已否定**；本轮门禁为真实 analyze/test。
- 全量 test 日志：`%TEMP%\drawing_notes_full_test.log`（及 tool-output 捕获）。
- 画布内容层（纸型网格、用户笔迹、PDF 导出）按 domain 豁免，仅 UI 铬 / 组件主题 / 内容卡片材质计违规。
- `docs/AUDIT_*.md` 与 `docs/audit_*.html` 按约定保持 **untracked**，不入库。

---

**结论一句话**：方向正确、回归可控；**合入前唯一硬阻塞是 P0-1 测试编译错误**，修 1 行后复跑双门禁即可放行候选；内容层玻璃与 9 处非法圆角建议同批清掉，避免设计令牌回潮。
