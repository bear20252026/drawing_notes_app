name: notebook_domain
description: Pure Dart notebook security domain for Drawing Notes V2 (session, key handle, lock policy, use cases, repository ports).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

# 纯 Dart——禁止 Widget/BuildContext/Platform/File 与具体存储实现（专家 R-02/R-03）。
dependencies: {}

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
